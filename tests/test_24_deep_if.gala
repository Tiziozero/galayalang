fn pick(a: u32, b: u32, c: u32): u32 {
    if a {
        if b {
            return b;
        } else if c {
            return c;
        } else {
            return a;
        }
    } else {
        return 0;
    }
}

fn main(): u32 {
    x := pick(1, 0, 5);
    return x;
}
