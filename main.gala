// @t <term> -> let <term> = fn_call()
struct vec{
    x: f32;
    y: f32;
};


fn doer_of_things(p: *vec) {
    (*p).x = 12;
    (*p).y = 8;
}
fn main() -> i32 {
    let p: vec = {x:1, y:2};
    p.x = 3;
    p.y = 2;
    doer_of_things(&p);
    return (p.x+p.y) as i32;
}
